[project]
name = "pctx-benchmarks"
version = "0.1.0"
description = "MCP-Bench benchmarking suite for pctx unified MCP model"
requires-python = ">=3.13"
dependencies = [
    "langchain>=0.3.0",
    "langchain-openai>=0.2.0",
    "langchain-core>=0.3.0",
    "mcp>=1.0.0",
    "requests>=2.32.0",
]
